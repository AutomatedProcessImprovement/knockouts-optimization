def evaluate_knockout_relocation_io(analyzer):
    return []


def evaluate_knockout_reordering_io(analyzer):
    # TODO: determine the order of knockouts from event log

    # TODO: determine if they are already ordered by increasing effort per rejection

    return []


def evaluate_knockout_rule_change_io(analyzer):
    return []
